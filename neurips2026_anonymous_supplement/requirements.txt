# Minimal dependencies for the supplement.
#
# Fast verification (scripts/verify_claims.py, check_manifest.py, make_table*.py,
# tools/check_anonymization.py) only needs the Python standard library.
# Optional figure regeneration needs matplotlib + numpy.

# --- core (always required) ---
# (none — stdlib only)

# --- figure regeneration (optional) ---
matplotlib>=3.7
numpy>=1.24
pandas>=2.0

# --- full rerun (optional) ---
# Uncomment if you intend to rerun model inference / training.
# torch>=2.0
# transformers>=4.40
# transformer_lens>=1.14
# trl>=0.8
# peft>=0.10
# accelerate>=0.27
# datasets>=2.14
# scikit-learn>=1.3
# einops>=0.7
# jaxtyping>=0.2
